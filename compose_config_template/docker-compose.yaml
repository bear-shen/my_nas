name: my_nas_compose

services:
  my_nas_compose:
    image: 0xee/my_docker_aio:latest
    container_name: my_nas_docker
    ports:
      - "{{WEBSITE_PORT}}:80"
      - "{{DB_PORT}}:5432"
    volumes:
      - ./nginx:/etc/nginx
      - {{APP_DIR}}:/myNas
      - ./postgresql:/etc/postgresql/16/main
      - ./php:/etc/php/8.3
      - ./supervisor:/etc/supervisor
      - ./crontab:/etc/cron.d
      - ./start.sh:/myDocker/start.sh
      # 注意compose的init.sh和docker的init.sh路径一致，会覆盖
      - ./init.sh:/myDocker/init.sh
    command: ["/bin/bash", "/myDocker/start.sh"]
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: {{POSTGRES_PASSWORD}}
      ONLYOFFICE_JWT_SECRET: {{ONLYOFFICE_JWT_SECRET}}
